# Darkwood Multiplayer Framework 0.8.7-alpha.20

本版修复 alpha.19 实机测试暴露的**快照应用失败**问题（FIX-007）。alpha.19 已把加载链完整打通（实测：客户端 11.2 秒加载完成、注册表稳定化 3229 实体、注册表握手发出、主机下发权威快照），但快照应用时客户端拒绝就绪——根因是**主机运行时生成物**首次进入实机视野。

## 根因（FIX-007）

主机世界里有 2 个对象（`deadbody_animal_border_main_cottageTrailer_01`、`ravens_border_main_cottageTrailer_01`，位于边境区块）是**本次会话运行时生成**的（乌鸦群、动物尸体），不写入存档——客户端加载主机存档后世界中没有它们。主机快照包含这 2 个共享容器，客户端绑定失败后按"有失败即阻断"的旧保护直接终止会话。

旧的"有失败即阻断"保护是为 alpha.14 灾难设计的（当时客户端注册表不完整，741 个容器中 738 个绑定失败）。本次实测失败率 2/739 = 0.27%，属于健康注册表 + 运行时实体差异——**这正是 0.8.8 Runtime Entity 生命周期（Spawn/Despawn 消息）要解决的欠账**，在 beta 阶段应容忍跳过而非阻断。

## 变更

- **快照绑定失败容忍阈值（FIX-007）**：失败数 ≤ max(10, 总数×5%) 时，缺失对象记入缺失实体集合、打警告并继续就绪；超过阈值仍阻断（保留 alpha.14 灾难保护）。实机数据 2/739 通过，738/741 仍阻断。
- **缺失实体集合**：快照/增量中无法绑定的实体 ID 记录在案，后续主机广播这些 ID 的容器状态时静默忽略（不再当作协议错误终止会话），等待 0.8.8 Spawn 生命周期补发。
- 阈值函数抽为 Core 纯函数，新增 3 项 SelfTests（58 → 61）。
- 版本号 0.8.7-alpha.20（双方须同版本）；协议 wire 无变更。

## 验证状态

- `dotnet build` 0 warning / 0 error；SelfTests 61/61。
- alpha.19 实机证据：FIX-006 生效（完成回调 11.2 秒触发、timeScale 恢复、注册表 3229/3233、快照 396KB 传输完成）；失败对象名称/坐标/候选数与根因分析完全吻合。
- 双端实机复验：待测（真机矩阵 VERIFY-001 第一关第 7-8 步）。

版本：0.8.7-alpha.20。
