# Darkwood Multiplayer Framework 0.8.8-beta.3

**Container Revision 并发冲突保护收口**——堵住"双客户端同时拿取复制物品"的漏洞。

## 本版新增：容器并发乐观锁（Container Revision）

背景：信任模式下容器操作由客户端本地直接执行并上报。当两个客户端**同时**操作同一容器时（A 拿 3、B 拿 5，都基于同一容器状态），主机此前会依次接受两份上报，导致世界总物品数凭空增加（复制物品）。

现在：

| 环节 | 行为 |
|---|---|
| 客户端上报 | 携带"基于的主机版本"（本地已知版本 + 1） |
| 主机乐观锁 | 版本匹配才应用（`ContainerRevisionGate.TryAdvance`）；**并发冲突（版本过期）→ 拒绝该次上报并回权威状态** |
| 客户端补偿 | 拿取时记录待确认物品（槽快照）；收到权威回滚 → **自动从背包退回未生效的物品**，容器回滚为权威版本；收到正常确认 → 清记录 |
| 广播 | 主机应用后广播给**所有**客户端（含上报者——作为确认包） |

并发语义：先到先得。后到的客户端容器和背包都回滚到操作前状态，物品总量守恒。

## 验证

- 构建 0 警告 / 0 错误；SelfTests **81/81**（新增：版本匹配推进、过期拒绝、A/B 并发先到先得）。
- 回环自测全链路通过（握手 → 真实存档 1,013,857 字节 SHA-256 → 快照 → 档案 → READY，9 秒）。

## 完整变更链（0.8.8-beta.1 → beta.3）

- beta.1：Runtime Entity 全链（协议/注册表/容器 35 米门控一次性动画/敌人 15Hz 代理）、默认出生点（FIX-013）、场景切换自动重连、回环自测、FIX-014。
- beta.2：发布打包清理（仓库只留当前版本 notes）。
- beta.3（本版）：Container Revision 乐观锁 + 并发补偿。

## 使用指南

快捷键：F6 面板 / F1 创建主机 / F2 加入 / F3 停止 / F4 营救 / F7-F8 回环自测。详见 README 与仓库内 ARCHITECTURE-ROADMAP.zh-CN.md。

## 已知边界

- 开服前已生成的运行时对象仍走快照容忍路径。
- 自测路径（quickLoadGame）出生点回退 (0,0,0) 与快照实体数偏小——快速读档时序特性，真实客户端不受影响。
- 双机实机矩阵（出生点/范围门控/敌人代理/场景切换/并发容器）待测试轮验证。
