# Darkwood Multiplayer Framework 0.8.7-alpha.12

本版实现 **HOTJOIN-001 热加入与访客档案**：主机开局后仍可随时加入新玩家；每名访客拥有稳定身份，物品与位置跨断线/重连由主机持久化保存。

## 变更

- **玩家身份**：`ClientHello` 携带访客身份（guest key，64 UTF-8 字节上限）。F6 面板新增"玩家名称"；留空自动生成唯一随机名。wire 变更 → 框架版本递增（无向下兼容策略，PROTO-001）。
- **影子库存修复**：此前只有第一个就绪的访客获得影子库存，第 2 名及以后的玩家所有拾取/存取会被 `PLAYER_INVENTORY_MISSING` 拒绝。现每名访客就绪时都按其访客档案创建影子库存。
- **访客档案**：主机侧按（主机存档令牌, 访客身份）持久化——背包/快捷栏槽位、最后位置、加入天数与次数。存储位于 `BepInEx\DarkwoodMPGuestProfiles\`，独立于存档目录，不会随存档传输泄漏。断线、主机 F3、每 30 秒自动落盘。
- **GuestProfile 消息**：快照应用后、就绪确认前，主机下发访客出生点与库存，客户端覆盖本地角色（位置 + 库存 + 清刚体速度）。
- **初始装备**：新增三档配置（`GuestStarterKitTier1/2/3`，按 `GuestStarterKitTier2Day=3`、`Tier3Day=7` 分档；`物品类型:数量` 分号分隔；留空不发）。仅首次加入发放，重连恢复档案、不重发。
- **出生点**：首次加入出生在主机旁（按加入次序圆周偏移 2 米）；重连回到档案最后位置。
- **容量校验**：握手时校验就绪访客数，超出（联机人数 - 1）返回 `SESSION_FULL` 拒绝码。
- 其余功能基线不变（主机权威物品事务、近战/门窗/物品交互权威闭环、怪物死亡镜像，自 alpha.10 引入，双端实机仍待验证）。

## 版本与兼容性

- 框架版本 `0.8.7-alpha.12`；Envelope 协议版本 3（常数）。
- **无向下兼容**：alpha.12 与 alpha.11 及所有更早版本互不兼容，双方必须使用同一版本安装包。

## 验证状态

- `dotnet build` 0 warning / 0 error；SelfTests 50/50 通过（退出码 0），新增：访客身份往返、GuestProfile 消息/档案往返与版本校验、SESSION_FULL 拒绝、双客户端容量、访客身份环回。
- 双端实机未验证：**IMPLEMENTED_UNVERIFIED**。

版本：0.8.7-alpha.12。
