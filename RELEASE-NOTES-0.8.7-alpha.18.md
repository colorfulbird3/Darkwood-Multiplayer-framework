# Darkwood Multiplayer Framework 0.8.7-alpha.18

本版解决客户端加载存档**长时间卡在低百分比（0%）**的问题（FIX-005）：主机静态存档 `savs.dat` 实测 9.0MB，其中 **63% 是 A* 导航图**（`graph` 字段，base64 编码的 ZIP）。客户端是视觉镜像（怪物 AI 冻结、无本地寻路），根本不需要导航图，但弱机解析 9MB JSON 中的巨型图数据耗时极长——这是低百分比阶段卡顿的物理根源。

## 变更

- **主机剥离导航图打包（FIX-005）**：向客户端发送存档时，`savs.dat`/`savs.dat_bak` 的 `graph` 字段替换为空（实测 9.0MB → 3.3MB，-64%；17429 个世界对象完整保留；本地已用真实存档验证 JSON 合法）。主机自己的存档不受影响。
- **客户端跳过图反序列化**：`AstarData.DeserializeGraphs` 在联机存档加载期间直接跳过（配合剥离后的空图，防止 A* 内部异常或阻塞）。
- **加载进度时间线日志**：客户端每跨 10% 记录一次进度与耗时（如"存档加载进度 30%（已用 25 秒）"）——若仍卡住，日志直接给出卡点。
- 版本号 0.8.7-alpha.18（双方须同版本）；协议 wire 无变更（存档包内容优化，格式不变）。

## 验证状态

- `dotnet build` 0 warning / 0 error；SelfTests 54/54。
- 剥离逻辑已在本机用真实主机存档验证（正则命中 1 处、JSON 解析通过、字段完整）。
- 双端实机未验证：**IMPLEMENTED_UNVERIFIED**（等待实机复验）。

版本：0.8.7-alpha.18。
