# Darkwood Multiplayer Framework 0.8.7-alpha.9

- 重做共享容器持久 `EntityId`：层级名称加入同名兄弟序号，组件加入同类型序号，减少双端碰撞和错绑。
- 世界快照为容器附带名称、位置和类型；客户端 ID 不一致时会按特征重新绑定到主机 ID。
- 共享容器快照若仍有容器无法应用，客户端会明确失败，不再错误显示“联机已就绪”。
- 鼠标与手柄的抓取、放置、堆叠和交换入口统一纳入主机权威事务，本地不再先行修改柜子。
- `ContainerPut` 支持指定目标槽位；不同物品拖放支持主机原子交换，成功后回传完整玩家库存和容器状态。
- 自动识别并迁移 alpha.8 的旧柜子扩容账本，升级后不会因 EntityId 变化再次把已有物品乘以联机人数。
- 协议版本升级到 3、快照架构升级到 3；主机和客户端必须同时使用 alpha.9。
- 完成实体权威审计：怪物位置、生命、存活/动作状态以及门窗状态继续由主机 15 Hz 下发；客户端怪物 AI 会被冻结。客户端攻击、门窗交互和剧情事件尚未形成完整 Action 请求闭环，后续版本继续实现。

版本：0.8.7-alpha.9。
