# Darkwood Multiplayer Framework — Hermes 项目上下文

## 项目身份

这是 Darkwood 的 BepInEx/Harmony 多人联机框架，当前基线 `0.8.7-alpha.10`。Hermes + DS V4 Pro 在本项目中是开发助手和模型，不是游戏运行时依赖。除非用户提供真实 SDK，否则不要创建或引用猜测的 Hermes/DS V4 网络 API。

## 工作目录

- 源码：`F:\SteamLibrary\steamapps\common\Darkwood\Darkwood Multiplayer framework\src`
- 游戏插件：`F:\SteamLibrary\steamapps\common\Darkwood\BepInEx\plugins`
- 编译输出：`F:\Private Blog\DMF Local Build\bin`
- 安装包：`F:\SteamLibrary\steamapps\common\Darkwood\Darkwood联机框架-安装包-v0.8.7-alpha.9`
- ZIP：`F:\SteamLibrary\steamapps\common\Darkwood\Darkwood联机框架-安装包-v0.8.7-alpha.9.zip`

## 不变量

1. Host 是世界、实体和共享物品的唯一权威。
2. Client 只发送 intent/action，不直接修改共享柜子或 Host 世界。
3. Action 必须有 RequestId、目标 EntityId、ExpectedRevision，并支持幂等重放。
4. Snapshot 未完整校验或共享容器绑定失败时，Client 不得 READY。
5. Persistent EntityId 禁止使用 `GetInstanceID()`、名字或 Instantiate 顺序。
6. 物品拿取、放置、拖放、堆叠和交换优先级最高；任何新功能不能破坏这条链路。
7. alpha.9 握手宣称 Protocol v3、Save Schema v1、Snapshot Schema v3；但当前 SaveBundle wire 为 v3、WorldSnapshot wire 为 v2。改动前先统一公共常量或明确拆分版本，并补 roundtrip/negative tests。

## 修改边界

- 可修改 `src/` 和本地文档。
- 只有用户明确要求时才覆盖正式游戏 DLL、生成 ZIP、启动游戏、上传 GitHub 或创建 Release。
- 不要上传 `Assembly-CSharp.dll`、BepInEx 核心、反编译源码或含第三方授权不明的二进制。
- 不使用 `git reset --hard`、`git checkout --` 或破坏性删除。

## 每次开发流程

1. 先读相关源码和现有日志，不凭模型猜 Darkwood 方法签名。
2. 协议改动先更新 DTO/codec 和 SelfTests，再改运行时。
3. 执行：

```powershell
dotnet build '.\src\DarkwoodMultiplayerFramework.sln' -c Release --no-restore -m:1 -p:MSBuildEnableWorkloadResolver=false
dotnet run --project '.\src\DarkwoodMultiplayerFramework.SelfTests\DarkwoodMultiplayerFramework.SelfTests.csproj' -c Release --no-build -p:MSBuildEnableWorkloadResolver=false
```

4. 报告修改文件、协议版本、测试结果和是否覆盖了 DLL/安装包。
5. 物品相关改动必须检查：快捷转移、`grabItem`、`controllerPickUpItem`、`placeItem`、`controllerPlaceItem`、`addToItem`、`swapItems`。

## 当前优先路线

- P0：物品主机权威事务的双端真实测试。
- P0：修复 SelfTests 的旧 0.8.6/protocol 1 fixture，并消除握手与 SaveBundle/WorldSnapshot wire 的 schema 漂移。
- P1：攻击 Request → Host 验证 → 伤害/死亡/掉落 → 结果同步。
- P1：门窗、陷阱、发电机和剧情事件 Action 化。
- P2：RuntimeEntityId 的 Spawn/Destroy/Reconnect 完整闭环。
- P3：在拿到真实 SDK 后增加新的传输适配器，保留 Telepathy fallback。

## 不要声称已完成

当前只确认怪物/门窗状态镜像和客户端 AI 冻结；没有完整攻击、门窗交互或剧情事件权威 Action。没有真实 Hermes/DS V4 SDK 文档时，也不能声称已完成运行时迁移。
