# Darkwood Multiplayer Framework 0.8.7-alpha.23

本版落实用户要求：**客户端打开容器、拿取/放入物品完全本地执行，不再经过主机审批**；执行后把容器状态发给主机，主机应用到自己的世界并广播给其他端。alpha.22 实机中"容器拿取被主机以 INVENTORY_FULL 拒绝、随后客户端卡死"的故障路径被整体移除。

## FIX-012：容器交互客户端本地权威（信任模式）

**之前**：客户端拿取/放入 → 发请求 → 主机审批（校验影子库存容量）→ 执行 → 广播。影子库存槽位记录与客户端实际背包不符 → 误判"库存已满"→ 拒绝 → 客户端 UI 等待不存在的批准 → 卡死。

**现在**：
- 客户端对共享容器的**所有操作本地立即执行**（transferItemToPlayer / transferItemTo / grabItem / placeItem / addToItem / swapItems 全部放行），执行后上报容器新状态。
- 主机收到客户端上报的容器状态 → 应用到自己的世界柜子 → **转发给其他玩家**（不回发给发送者）。
- 主机玩家自己的容器操作保持原样（本地执行 + 广播）。
- **删除主机端 ContainerTake/Put 的审批执行逻辑与全部拒绝路径**（INVENTORY_FULL 等）。
- 影子库存改为**动态扩槽**（信任模式：除非法输入外不判满），拾取与攻击路径也不再误拒。

## 附带验证（alpha.22 实机日志）

- 加载链全通（11.5 秒，FIX-006 生效）；快照 753/755（2 个运行时生成物按 FIX-007 容忍跳过）。
- 开柜子（ItemActivate）已被主机正常应用（isOn=False）——alpha.22 的信任模式生效。
- 唯一卡点是容器拿取 INVENTORY_FULL——本版已整体移除该路径。

## 验证状态

- `dotnet build` 0 warning / 0 error；SelfTests 61/61。
- 协议 wire 无变更（复用 InventoryState 消息承载客户端容器状态上报）。

版本：0.8.7-alpha.23。
