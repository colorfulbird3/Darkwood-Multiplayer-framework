# Darkwood Multiplayer Framework 0.8.7-alpha.13

本版实现 **DOWNED-001 倒地与营救**：玩家阵亡不再直接进入原版死亡流程——只要还有其他玩家存活，阵亡玩家进入**倒地状态**（无法行动、视角固定在原地），可被队友营救复活；**全员阵亡**时触发游戏原版结局并结束联机会话。

## 变更

- **倒地状态**：拦截原版 `Player.die()`/`onDeath()`（Harmony）。有队友存活时跳过原版死亡流程，进入倒地：禁止一切输入（`Core.forbidInputs`）、刚体冻结、视角停留在原地、攻击者被驱散、角色躺下。倒地期间免疫进一步伤害。
- **玩家血量主机权威**：主机持有所有玩家血量（自身实时读取 + 访客影子血量）。新增 `PlayerHealth` 消息（变化即广播 + 1 秒心跳）；客户端只应用自己 id 的血量。`PlayerPose` 携带 `MaxHealth` 与倒地 flag。
- **怪物 → 访客伤害**：主机按 0.25s 扫描：存活且非中立的主机怪物在访客 1.6 米内时，按怪物近战伤害（`sensorTypes[0].damage`）扣访客影子血量（每 0.5s 冷却）。倒地访客免疫。
- **营救**：存活玩家按 **F4** 向主机发起营救请求（再按一次取消）。主机校验后选定 2.5 米内最近的倒地队友，开始 **3 秒**营救计时；营救期间营救者与倒地者均锁定，主机每 0.1s 广播进度；倒地玩家头顶显示营救进度条（`DarkwoodRescueOverlay`）。取消/死亡/断线/超距 → 进度归零双方解锁。
- **复活效果**：被救起者恢复**生命上限的 10%**、体力回满，并带 3 秒无敌保护（防止起身被围殴）；行动与视角恢复正常。
- **全员阵亡**：主机判定所有玩家倒地后广播 `AllDowned`，每台机器触发**游戏原版死亡结局**（按难度：梦魇/困难无命永久死亡视频，普通难度回家复活），2 秒后结束联机会话。
- 热加入：新加入者满血出生；断线重连不再继承倒地状态（满血复活）。
- 其余功能基线不变（主机权威物品事务、近战/门窗/物品交互权威闭环、怪物死亡镜像、热加入访客档案，自 alpha.10-12 引入，双端实机仍待验证）。

## 版本与兼容性

- 框架版本 `0.8.7-alpha.13`；Envelope 协议版本 3（常数）。
- **无向下兼容**：alpha.13 与 alpha.12 及所有更早版本互不兼容，双方必须使用同一版本安装包。

## 验证状态

- `dotnet build` 0 warning / 0 error；SelfTests 54/54 通过（退出码 0），新增：PlayerHealth/RescueRequest/RescueProgress/AllDowned 往返、pose 携带 MaxHealth 与倒地 flag、GuestProfile 血量字段。
- 双端实机未验证：**IMPLEMENTED_UNVERIFIED**（倒地视觉、营救手感、怪物近战近似伤害均需实机调参）。

版本：0.8.7-alpha.13。
