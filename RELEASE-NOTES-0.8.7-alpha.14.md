# Darkwood Multiplayer Framework 0.8.7-alpha.14

本版修复首次双端实机暴露的**快照容器绑定失败**（FIX-001）：客户端加载主机存档后，世界对象仍会流式生成，但客户端实体注册表在 `Player.Instance` 出现时就被过早构建且不再重建，导致快照中 741 个共享容器只有 3 个能绑定（738 个失败），客户端被安全机制拦在 Ready 之前。

## 变更

- **注册表稳定化（FIX-001）**：客户端就绪前进入稳定化循环——反复强制重建实体注册表，直到实体数连续 3 次（间隔 1 秒）不再变化（最长 90 秒），确保注册表覆盖主机世界的完整对象集后才发送 Ready。同时新增门控，防止 Update 循环在稳定化完成前提前发送 Ready。
- **容器重绑定容差放宽**：快照库存无法按 ID 直接绑定时，按（类型 + 名称 + 位置）重绑定的距离容差从 0.5 米放宽到 1.0 米（应对存档序列化可能带来的微小坐标漂移）。
- **失败诊断日志**：绑定失败时前 8 条会附带客户端候选详情（共享容器总数、同类型/同名最近距离），错误汇总带客户端共享容器计数——若仍复现，两端 `BepInEx\LogOutput.log` 可直接定位。
- **SelfTests**：`session capacity` 测试改为顺序连接（消除双客户端并发 Hello 的竞态闪断）；54/54 连续 3 轮全过。
- 其余功能基线不变（alpha.13 全部能力）。

## 版本与兼容性

- 框架版本 `0.8.7-alpha.14`；Envelope 协议版本 3（常数）。本版**无 wire 变更**（纯客户端加载时序与诊断），但按单一版本门槛策略，双方仍须使用同一版本。

## 验证状态

- `dotnet build` 0 warning / 0 error；SelfTests 54/54（连续 3 轮）。
- 双端实机未验证：**IMPLEMENTED_UNVERIFIED**（本版即为修复双端问题的版本，等待实机复验）。

版本：0.8.7-alpha.14。
