# Darkwood Multiplayer Framework 0.8.7-alpha.6

- 修复主机创建时一次性处理数百个柜子导致游戏卡到个位数帧的问题。
- 共享柜子扩容改为分帧协程，每帧只处理少量库存，避免阻塞 Unity 主线程。
- 主机可以先完成握手；WorldSnapshot 会在柜子准备完成后发送，避免客户端连接超时。
- 保留 alpha.5 的共享库存、人数倍率和持久化扩容账本。
