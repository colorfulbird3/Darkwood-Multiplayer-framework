# BearTrap 状态同步（v0.9.2）

## 状态

Fixed in code — awaiting real-machine verification（real Component type 反编译已完成；占位实现 → 真实字段）

## 字段

Armed / Triggered / Occupied / Broken / Enabled / ColliderEnabled / TriggerEnabled / VisualState

## 识别

用真实 Component type（TriggerBlocker），不用 name contains "beartrap"。

## 真机验收

TEST G：BearTrap
