# VERIFY-002 — 0.8.8/0.8.9 验证记录

## 结论：PASS（回环全链路）+ 实机矩阵待测

## 0.8.9-beta.3 重构验证（2026-08-17）

十刀架构重构（partial 拆分、SessionContext、MessageRouter、协议领域文件、Transport 真接口、Host/Client Tick 分离、Runtime Entity 生命周期模型、xUnit 项目）后复测：

- 构建 0 警告 / 0 错误
- SelfTests 81/81（原有全套协议/组件测试保留）
- xUnit 8/8（tests/DarkwoodMultiplayerFramework.UnitTests：乐观锁 / ID 纪律 / 生命周期 / codec / SessionContext）
- 回环自测全链路通过（握手 → 存档 1,022,925 字节 SHA-256 → 快照 895 字节 → 档案 → READY，9 秒）
- 消息路由无"未注册类型"警告（25 种消息全部被 handler 认领）

## 已验证据

### 1. 回环自测全链路（本机实测，2026-08-16）

配置 `SelfTestAuto=true` 启动游戏，自动执行完整联机协议链：

```
0s  启动 → 3s 自动开主机（监听 17777）→ 5s 自动读档 → 6s 主机 READY
6s  回环握手成功（版本检查通过）
6-9s 真实主机存档传输 8 块（1,013,859 字节）→ SHA-256 校验通过
9s  权威世界快照（895 字节）→ SHA-256 校验通过
9s  访客档案（出生点/天数/加入次数）→ READY 确认
9s  ✓✓ 自测通过
```

- 运行两次均通过；第二次 join 计数从 1 → 2（访客档案热加入持久化验证）。
- 证据日志：`BepInEx/LogOutput.log`（[自测 Ns] 时间戳链路）。

### 2. 自动化测试

- SelfTests 78/78（协议 roundtrip、注册表纪律、事件门控一次性语义、codec 严格性、场景切换消息）。

### 3. alpha.23 真机基线（上一轮 VERIFY-001）

- 容器状态同步、开关门、物品开关真机通过——本版未回退这些路径（回归由 SelfTests + 回环自测覆盖）。

## 未覆盖待测（实机矩阵，随 beta 测试轮执行）

| 项 | 状态 |
|---|---|
| FIX-013 默认出生点（真实客户端） | 待双机验证 |
| 乌鸦/尸体范围门控 + 一次性动画 | 待双机验证 |
| 运行时敌人代理（15Hz delta） | 待双机验证 |
| 场景切换自动重连 | 待双机验证（需 chapter1→2 进度） |
| 热加入运行时实体门控 | 待双机验证 |
| 长时运行稳定性 | 0.8.9 计划 |

## 已知边界（不阻断）

- 开服前已生成的运行时对象仍走快照容忍路径。
- 自测路径（quickLoadGame）出生点回退 (0,0,0) 与快照实体数偏小——快速读档时序特性，真实客户端不受影响。
