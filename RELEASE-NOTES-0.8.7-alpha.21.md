# Darkwood Multiplayer Framework 0.8.7-alpha.21

**里程碑版**：alpha.20 实机测试实现了首次完整双端连通——客户端 READY、双方互相可见、主机→客户端事件同步（开门等）全部工作。本版修复三个问题：**主机存档损坏（严重）**、日志刷屏、柜子交互被拒的诊断。

## FIX-008：主机存档损坏（最高优先级）

**根因**：共享柜子扩容账本的主机身份令牌用了 `savs.dat` 的**文件创建时间**。游戏每次保存都会重写该文件（创建时间变化）→ 账本在下次开服时全部失效 → 758 个柜子被**再次翻倍物品**。多次测试后物品量指数膨胀、槽位溢出，`Scale` 的兜底逻辑还会把剩余量堆回原槽（**超过物品 maxAmount**）→ 写入存档 → 游戏读档校验失败 →"存档损坏、无法进入"。

**修复**：
- 账本身份令牌改为稳定值（存档目录名 + profile id），不再随文件重写变化——每个柜子每档只扩容一次。
- `Scale` 兜底改为"无空槽即停止复制"，绝不超限堆叠。
- 已受影响的存档：物品数量无法自动还原（原始值未记录）。若读档失败，游戏会自动尝试 `_bak` 备份；若读档成功但物品偏多，属历史扩容残留，alpha.21 起不再恶化。

## FIX-009：日志刷屏

主机每帧发送队列清空时打印"已完成向玩家发送排队数据"，15Hz 同步下每秒数十行、日志膨胀到数万行。该日志已移除。

## FIX-010：柜子交互被拒（TOO_FAR）诊断

客户端开柜子（ItemActivate）被主机以 TOO_FAR 拒绝。本版在拒绝点记录**具体距离、玩家镜像坐标、物品坐标**——复测时若仍被拒，日志会直接给出偏差来源（是镜像位置未同步，还是物品坐标分叉），一次测试即可定位。

## 验证状态

- `dotnet build` 0 warning / 0 error；SelfTests 61/61。
- alpha.20 实机：加载链全通（FIX-006/007 生效）、首次双方可见、事件同步正常。
- 待复测：柜子交互（第二关）；关注 TOO_FAR 诊断日志。

版本：0.8.7-alpha.21。
